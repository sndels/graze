#ifndef GRAZE_RAY_HU
#define GRAZE_RAY_HU

#include "vec.hu"

struct Ray {
    Vec3 o;
    Vec3 d;
    float t_min;
    float t_max;

    __device__ Ray();
    __device__ Ray(const Vec3& o, const Vec3& d, const float t_min, const float t_max);
    __device__ Vec3 point(const float t) const;
};

#endif // GRAZE_RAY_HU
